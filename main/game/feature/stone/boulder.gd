extends Feature



func _ready() -> void:
	#var rng : RandomNumberGenerator = RandomNumberGenerator.new()
	#rng.seed = feature_seed
	#$mesh.mesh = load("res://assets/model/feature/stone/boulder/" + str(rng.randi_range(0, 4)) + ".obj")
	pass
